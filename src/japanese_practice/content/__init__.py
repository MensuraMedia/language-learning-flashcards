"""Bundled Japanese content: the character seed sets and the database loader.

Sub-modules are imported explicitly (``from .hiragana import HIRAGANA``) rather
than re-exported here, so that importing one seed set never drags in the others.
"""
